# Ryuk (Testcontainers' auto-cleanup helper container) hits a Docker API version
# mismatch on some Windows/Docker Desktop setups independent of DOCKER_API_VERSION.
# Disabling it is safe for local dev and CI — Docker Desktop / the CI runner both
# clean up stopped containers on their own; Ryuk is a convenience, not a requirement.
ryuk.container.disabled=true
